import { SideItemsDirective } from './side-items.directive';

describe('SideItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new SideItemsDirective();
    expect(directive).toBeTruthy();
  });
});
