import { MainItemsDirective } from './main-items.directive';

describe('MainItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new MainItemsDirective();
    expect(directive).toBeTruthy();
  });
});
